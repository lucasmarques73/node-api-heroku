name: CI

on:
  push:
    branches:
      - master

jobs:
  install_dependencies:
    runs-on: ubuntu-latest
    steps:
        uses: actions/checkout@v1
      - name: Install Dependencies
        run: npm install
